# Used by docker-compose.yml only (via ${VAR} substitution).
# The server/ and client/ folders each have their own .env.example for
# running them directly with `npm run dev` outside Docker.

JWT_SECRET=replace-with-a-long-random-secret
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
